{
    "type": "object",
    "schemaVersion": 1,
    "title": "Research MCP Server Input",
    "properties": {
        "preset": {
            "type": "string",
            "title": "Tool Preset",
            "description": "Select which tools to load: all (23 tools), web (6), social (9), academic (6), data (2)",
            "default": "all",
            "enum": ["all", "web", "social", "academic", "data"],
            "editor": "select"
        }
    }
}
